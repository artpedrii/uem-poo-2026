info:
  name: POST
  type: http

http:
  method: POST
  url: http://localhost:8080/pedidos
  body:
    type: json
    data: |-
      {
        "id" : 2, 
        "nome" : "SUPER VIRUS DO MAL"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
